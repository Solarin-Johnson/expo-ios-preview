import PreviewBase from "@/components/preview-base";
import EmptyState from "@/components/empty-state";

export default function Tab() {
  return (
    <PreviewBase>
      <EmptyState title={"Recent"} symbol="clock.fill" />
    </PreviewBase>
  );
}
